<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ypx.imagepicker">
    <!-- 读取媒体文件 -->
    <uses-permission android:name="com.android.providers.media.MediaProvider" />
    <!-- 往SDCard写入数据权限,将更新的APK临时存在SD卡里 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <!-- 在SDCard中创建与删除文件权限 -->
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" />
    <!-- 访问Internet权限 -->
    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.ypx.imagepicker.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--<activity-->
            <!--android:name="com.ypx.imagepicker.ui.activity.ImageCropActivity"-->
            <!--android:label="@string/app_name"-->
            <!--android:theme="@android:style/Theme.NoTitleBar" />-->


        <!--<activity-->
            <!--android:name="com.ypx.imagepicker.ui.activity.ImagePreviewActivity"-->
            <!--android:label="@string/app_name"-->
            <!--android:theme="@android:style/Theme.NoTitleBar" />-->

        <!--<activity-->
            <!--android:name="com.ypx.imagepicker.ui.activity.ImagesGridActivity"-->
            <!--android:label="@string/app_name"-->
            <!--android:theme="@android:style/Theme.NoTitleBar" />-->

        <!--<activity-->
            <!--android:name="com.ypx.imagepicker.ui.activity.SingleImagesGridActivity"-->
            <!--android:label="@string/app_name"-->
            <!--android:theme="@android:style/Theme.NoTitleBar" />-->
    </application>

</manifest>